[metadata]
name = hermes-agent-smart-inference
version = 0.1.0
description = Deterministic per-turn model routing for Hermes Agent

[options]
packages = find:
python_requires = >=3.10

[options.packages.find]
include = smart_inference*
